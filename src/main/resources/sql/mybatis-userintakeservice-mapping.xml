<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="UserIntakeMapper">

	<select id="getUserIntake" parameterType="userintakeVO" resultType="userintakeVO">
			SELECT
			a.no, a.id, a.code, a.count, a.calories, a.time, b.img
			FROM user_intake a JOIN food b
			ON a.code = b.code
			WHERE a.id = #{id} and a.time = #{time}
	</select>
</mapper>